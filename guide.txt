** Create files index.html, style.css and app.js files

**populate head, meta and title tags into document

**Get lato font from google using the link tag 

**Apply the lato css font-family property in the style.css files

** create img folder and populate with project images.

** populate the body element with nav tag objects and image

**input nav element styles in css file 

**input styles for navTop class element.

**input styles for serach class element

**input styles for searchInput class for input element

**style place holder in search input element using searchInput class

**style limited offer class

**populate element with navBttom class to populate the application links

** apply styles for navBottom class element

**apply styles to menuItem class

** create a new div after nav element with class of slider and populate sub divs

** apply styles for slider

** apply clip-path polygon styling to slider class using (https://bennettfeely.com/clippy/)

** duplicate sliderItem class element to form 5 individual components

** apply style to sliderWrapper class

** apply style to sliderItem class

**apply positioning styles to sliderTitle class

** apply positioning style for sliderPrice class

** apply styles for sliderBg class

** insert z-index into sliderTitle, sliderPrice and sliderImg classes

** apply style to buyButton class 

** apply hover styling to buyButton class

** remove background-color from slideBg class and style the sliderItem class using nth child implementation targeting the sliderBg class for the 5 items.

** apply style for sliderItem class using nth child implementation targeting sliderPrice class

** insert script element before the closing body tag of index.html file to bring in app.js

** Select the sliderWrapper class using a querySelector

** Select all items with the menuItem class using the querySelectorAll function

** perform a forEach loop over the menuItems to applay a css transform feature on the wrapper class items.

** remove the horizontal scroll bar by applying overflow: hidden property to the slider class style in style.css file

** add new features element in index.html file after the slider div element

** add individual feature elemtents under the features element

** apply css styles for features, feature, featureIcon, featureTitle and featureDesc classes

** create a new div for product class after the features div and populate

** apply styling for product, productImg, productDetails, productTitle, productDesc, colors, sizes, color, color:last-child, size, and productButton classes

** create an array of product objects in app.js file.

** create  variable for chosenProduct

** inside meunItems loop, assign chosen product to the index

** use querySelector to target and change the Product element and sub elements.

** change to currentProduct parameters with given object properties, title, price, img, color

** loop over each of the navigation menu items to apply css transform functionality when there is a click event.

** Assign chosenProduct to products index in the loop.

** change texts of currentProduct when the click event occurs.

** assign new colors on each item when the navigation click event occurs.

** create a seperate loop to control the change in product color when selected using the color element selector. change the image src when clicked.


**** Create functionality for buy button *******

** In index.html, add an id attribute to the product element.

** Add an anchor tag element to all BUY NOW buttons in sliderWrapper to link to the product component

** add smooth transition; go to style.css and add scroll behavior smooth styling to top level html tag


*** Shoe sizes ***
** Add a new loop  for currentShoeSizes in app.js to select shoe sizes


**** Buy now modal ****
** add a new div element with class of payment after productDetails element and populate from soure code

** addstyling for payment class in style.css. Also apply box shadow from "https://cssgenerator.org/box-shadow-css-generator.html" link

** style payTitle class

** style label element

** style payInput class

** style payInput placeholder

** create a new h1 element with class "card Information" after address label and input

** add div for card icons wih class of "cardIcons"

** add input element for password after cardIcons class element

** add a div element of class  "cardInfo" after password input and populate from source code 
** add a button element with class of paybButton after cardInfo element

** add a span element with a class of close after the checkout button

** In style.css apply styling for the following classes (cardIcons, cardIcon, cardInfo, sm, payButton and close)

** edit payment class styling, change display to none

** create querySelectors to target productButton, payment and close class elements.

** add eventListeners to productButton and close button class elements

** add a new element with a class of gallery just above the app.js script element and populate from source

** add stylying for galley, galleryItem, and galleryImg class elements 

** add a new element with a class of newSeason just above the script element and populate with first and third nsItem class elemtent 

** populate the newSeason class element with second nsItem class element and add an id of "nav" to the nav element above 

** add styling for newSeason, nsItem, nsImg, nsImg, nsTitle, and nsButton classes respectively

** add a new div element and populate its content from source

** add styling for footer section classes

** apply media queries styling for mobile devices


>>>> END <<<<<<